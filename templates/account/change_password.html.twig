{% extends 'layouts/base.html.twig' %}

{% block title %} Change Password {% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-6 mx-auto text-center">
            <h1 class="my-5 text-center" >Change Password</h1>
            
            {{ form_start(form) }}
                {{ form_widget(form) }}

                <button type="submit" class="btn btn-primary btn-lg btn-clock">
                    Update password
                </button>
                

            {{ form_end(form) }}


        </div>
    </div>
{% endblock %} 